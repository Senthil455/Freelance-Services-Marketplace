# Auth form notes

- keep big forms in a Card with a single submit button.
- use the `label` and `input` helper classes for consistent styling.
- disable the submit button while the request is in flight.
- toast.success / toast.error for feedback.
- redirect after a successful submit based on role.
